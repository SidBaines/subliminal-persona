diff --git a/runner/src/config.ts b/runner/src/config.ts
--- a/runner/src/config.ts
+++ b/runner/src/config.ts
@@ -6,3 +6,4 @@ export const OUTPUT_DIRECTORY =
 export const MAX_CONCURRENCY = Number(process.env.MAX_CONCURRENCY ?? 20);
 export const TEST_RUNS_PER_MODEL = Number(process.env.TEST_RUNS_PER_MODEL ?? 5);
 export const TIMEOUT_SECONDS = Number(process.env.TIMEOUT_SECONDS ?? 600);
+export const MAX_TOKENS = Number(process.env.MAX_TOKENS ?? 4096);
diff --git a/runner/src/openrouter/prompt-based.ts b/runner/src/openrouter/prompt-based.ts
--- a/runner/src/openrouter/prompt-based.ts
+++ b/runner/src/openrouter/prompt-based.ts
@@ -1,4 +1,5 @@
 import { createOpenRouterClient } from "./client";
+import { MAX_TOKENS } from "../config";
 import { withRetry, extractErrorDetails } from "./retry";
 import { FreeModelRateLimiter } from "./rate-limit";
 import {
@@ -93,6 +94,7 @@ export async function callOpenRouterWithPromptBasedTools(
         client.chat.completions.create({
           model,
           messages: allMessages as any,
+          max_tokens: MAX_TOKENS,
         }),
       {
         operationName: `PromptBased API call for ${model}`,
